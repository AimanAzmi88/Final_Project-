    <div>
      <ul>
        {data.map((item) => (
          <li key={item.id}>
            <p>ID: {item.id}</p>
            <p>Description: {item.description}</p>
            <p>Time: {formatTimestamp(item.timestamp)}</p>
            <p>Status: {(item.status).toString()}</p>
            <p>Completed: {item.completed ? 'Yes' : 'No'}</p>
            {!item.completed && (
              <button className='bg-button' onClick={() => handleComplete(item.id)}>Book Slot</button>
            )}
          </li>
        ))}
      </ul>
    </div>